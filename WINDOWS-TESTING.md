# Angel of Delusion 桌宠 · Windows 11 测试清单

> 适用范围：Windows 11 x64 实机测试
> 当前边界：GitHub Actions 能证明 Windows 工具链可编译；透明窗口、逐像素穿透、拖拽、DPI、托盘与开机启动必须按本文在实机确认。

## 1. 下载测试包

1. 打开 GitHub 仓库的 **Actions** 页面。
2. 选择最新成功的 **Windows x64 Build**。
3. 在页面底部下载名称以 `AngelofDelusion-deskpet-windows-x64-` 开头的 Artifact。
4. 解压后应包含：
   - `*-setup.exe`：NSIS 安装程序，作为日常测试的首选。
   - `*.msi`：MSI 安装程序，用于企业安装和 MSI 专项验证。
   - `BUILD-INFO.txt`：commit、测试版本和 Actions 运行编号。
   - `SHA256SUMS.txt`：安装包校验值。
   - 本测试清单。
5. 记录 `BUILD-INFO.txt` 中的 `Commit` 和 `Test version`；回报 Bug 时必须附上。

可选校验，在解压目录打开 PowerShell：

```powershell
Get-FileHash -Algorithm SHA256 .\*.exe, .\*.msi
Get-Content .\SHA256SUMS.txt
```

两处哈希应一致。

## 2. 测试环境记录

测试前填写：

| 项目 | 记录 |
|---|---|
| 测试日期 |  |
| 测试人 |  |
| Commit |  |
| Test version |  |
| Windows 版本 | `Win + R` → `winver` |
| 架构 | x64 / ARM64 |
| CPU / GPU |  |
| 显示器 | 单屏 / 双屏及分辨率 |
| Windows 缩放 | 100% / 125% / 150% / 200% |
| 安装包 | NSIS / MSI |

## 3. 安装规则

- 第一轮优先使用 NSIS `*-setup.exe`。
- NSIS 与 MSI 不要同时安装；切换安装器前先卸载现有版本。
- 测试包暂未签名，Windows SmartScreen 可能提示未知发布者。只运行从本私有仓库 Actions 下载且 SHA-256 匹配的安装包。
- 正常覆盖测试使用更高的 `Test version`，不需要手动删除配置。
- 配置通常位于 `%APPDATA%\com.hana.zzzidolpet\settings.json`。

## 4. 首次启动冒烟测试

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-001 | 安装后启动 | 桌宠出现，无白底、黑框或命令行窗口 | ☐ |
| W-002 | 查看任务栏 | 主桌宠不占普通任务栏按钮 | ☐ |
| W-003 | 查看系统托盘 | 存在桌宠托盘图标和菜单 | ☐ |
| W-004 | 打开设置 | 设置窗口正常显示且可以滚动 | ☐ |
| W-005 | 分别切换三名角色 | 爱芮、南宫、千夏均完整加载 | ☐ |
| W-006 | 关闭设置窗口 | 只隐藏设置，桌宠继续运行 | ☐ |
| W-007 | 托盘隐藏/显示桌宠 | 隐藏和恢复均正常 | ☐ |
| W-008 | 托盘退出 | 主窗口、设置窗口和托盘图标全部退出 | ☐ |

## 5. 渲染、表情和视线

三名角色分别执行：

| 编号 | 检查 | 预期 | 结果 |
|---|---|---|---|
| W-101 | 待机 30 秒 | 动画流畅，无闪烁、黑影或槽位残留 | ☐ |
| W-102 | 单击、双击、连续点击 | 动作与绑定表情正确，结束后恢复待机 | ☐ |
| W-103 | 快速经过角色 | 表情不会被异常永久替换 | ☐ |
| W-104 | 鼠标在角色四周移动 | 眼神平滑跟随且不跑出眼框 | ☐ |
| W-105 | 关闭视线追踪 | 眼睛回正且不再跟随 | ☐ |
| W-106 | 重新打开视线追踪 | 立即恢复跟随 | ☐ |
| W-107 | 调整人物到 60%、100%、125% | 人物保持清晰、居中且不被裁切 | ☐ |

## 6. 透明窗口与逐像素穿透

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-201 | 点击人物周围透明区域 | 点击落到后方应用或桌面 | ☐ |
| W-202 | 从透明区域移入头发、手脚和身体 | 角色恢复可点击 | ☐ |
| W-203 | 点击半透明发丝边缘 | 命中稳定，不快速闪烁切换 | ☐ |
| W-204 | 角色倾斜或物理摆动时测试边缘 | 命中区域跟随当前可见像素 | ☐ |
| W-205 | 打开设置或调试控件 | 控件始终可操作，不被穿透 | ☐ |

## 7. 拖拽和布娃娃摆动

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-301 | 从头、身体、腿分别拖动 | 均像从头顶固定抓点拎起 | ☐ |
| W-302 | 小范围慢速移动 | 不抽搐、不抖动 | ☐ |
| W-303 | 左右快速晃动 | 整体角度随速度和惯性变化 | ☐ |
| W-304 | 松开鼠标 | 带惯性回正，并恢复待机 | ☐ |
| W-305 | 拖出窗口原区域后松开 | 不锁死，仍可再次点击和拖动 | ☐ |
| W-306 | 靠屏幕四边拖动 | 人物不永久丢失到工作区外 | ☐ |

## 8. 自动漫步

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-401 | 等待向左漫步 | 朝左正常前进且不闪烁 | ☐ |
| W-402 | 等待向右漫步 | 使用朝左资源镜像后正常前进，无太空步 | ☐ |
| W-403 | 漫步时点击 | 立即停止移动并播放反馈 | ☐ |
| W-404 | 漫步时拖拽 | 自动移动中断，不与拖拽争夺位置 | ☐ |
| W-405 | 关闭自动漫步并等待 30 秒 | 窗口不再主动移动 | ☐ |
| W-406 | 调整频率与速度 | 下一轮漫步的间隔和速度明显变化 | ☐ |
| W-407 | 到达任务栏附近 | 不进入任务栏占用的工作区 | ☐ |

### 集合移动回归

先将三名角色放在同一显示器的不同高度，关闭调试面板并确保角色未处于托举状态。刷新率指 Windows 显示设置中的显示器刷新率。

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-408 | 关闭自动漫步，分别在 60Hz 和 144Hz/240Hz（若支持）点击“集合角色” | 三名角色持续向目标移动并排齐；爱芮不再原地踏步 | ☐ |
| W-409 | 将角色放在水平相距较远、垂直只差几十像素的位置后集合 | 横纵方向均有进度，不出现只横移、最后卡住 | ☐ |
| W-410 | 将角色分散到同屏两侧，速度设为最低，再执行集合 | 远距离移动及时到位，未因 8 秒场景超时停在半路 | ☐ |
| W-411 | 集合过程中拖拽角色，松开后再次集合 | 拖拽能打断集合，旧移动不会恢复；再次集合正常 | ☐ |
| W-412 | 将窗口拖到屏幕边缘，使部分透明区域越出工作区，再点击集合 | 回到工作区后继续走向集合位置，不在边界提前停止 | ☐ |
| W-413 | 打开角色右键菜单，停留超过 2 秒后选择集合；同时让一名角色播放待机小动作 | 可见且空闲的三名角色均参与，每个角色只收到一次对应场景指令 | ☐ |

### Windows 顶部拖拽回归

关闭自动漫步，将角色缩放设为 0.6；不使用模拟拖拽按钮，执行真实鼠标拖拽。

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-414 | 抓住人物身体，拖到头顶靠近屏幕上沿后松手 | 上方透明留白可以越出屏幕，人物不会被挡在屏幕上部约 200 像素以下，也不会松手回弹 | ☐ |
| W-415 | 在顶部位置重启；拖动到不同 DPI 的副屏；将保存位置所在副屏断开再重启 | 有效顶部位置保留，跨屏抓取点跟随鼠标，移除副屏后角色可找回 | ☐ |
| W-416 | 单击、双击、快速连续点击；拖拽时停住鼠标但不松手；按 Esc 取消拖拽 | 点击反馈正常；未松手不会结束拖拽；Esc 返回本次拖拽起点 | ☐ |

Windows 日志中的 `desktop_pet::drag` 会记录每次拖拽的起止坐标和取消状态。负的窗口 Y 坐标表示透明留白可以越过屏幕顶部，并不等于人物不可见。

若仍有角色不参与集合，复现后导出诊断报告。日志中的 `layout requested`、`move_requested`、`move_start_failed`、`move_arrived` 和 `cancelled` 会保留参与名单、目标位置及结束原因。

## 9. DPI 与多显示器

至少测试当前设备支持的缩放档位。更改 Windows 缩放后，注销再登录或重启应用再复测。

| 编号 | 场景 | 预期 | 结果 |
|---|---|---|---|
| W-501 | 100% 缩放 | 渲染、点击、穿透和拖拽坐标一致 | ☐ |
| W-502 | 125% 缩放 | 同上 | ☐ |
| W-503 | 150% 缩放 | 同上 | ☐ |
| W-504 | 200% 缩放（若可用） | 同上 | ☐ |
| W-505 | 双屏相同 DPI | 可跨屏拖拽，位置恢复正确 | ☐ |
| W-506 | 双屏不同 DPI | 跨屏后大小、命中和拖拽不偏移 | ☐ |
| W-507 | 副屏放在主屏左侧 | 负坐标下拖拽和漫步正常 | ☐ |
| W-508 | 拔掉保存位置所在副屏后启动 | 回退到主显示器可见工作区 | ☐ |

## 10. 设置、托盘和重启恢复

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-601 | 设置页关闭自动漫步 | 托盘勾选同步取消，运行时立即停止 | ☐ |
| W-602 | 托盘开启自动漫步 | 设置页同步开启 | ☐ |
| W-603 | 切换置顶开关 | 窗口层级立即变化且两处状态一致 | ☐ |
| W-604 | 修改角色、大小、性能和帧率后重启 | 所有设置保留 | ☐ |
| W-605 | 拖到新位置后重启 | 恢复到上次有效位置 | ☐ |
| W-606 | 启用开机启动并重启 Windows | 登录后桌宠自动运行一次 | ☐ |
| W-607 | 关闭开机启动并重启 Windows | 不再自动运行 | ☐ |
| W-608 | 将 `settings.json` 改成无效文本后启动 | 备份损坏文件并以默认设置启动 | ☐ |

完成 W-608 后，可删除测试生成的 `settings.corrupt-*.json`；不要在回报 Bug 前删除仍需分析的文件。

## 11. 性能与稳定性

在任务管理器中观察桌宠进程：

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-701 | 平衡 / 自动运行 10 分钟 | CPU、内存稳定，无持续增长 | ☐ |
| W-702 | 省电 / 自动运行 10 分钟 | 帧率约 30 FPS，资源占用下降 | ☐ |
| W-703 | 三角色往返切换 30 次 | 不崩溃、无明显内存持续增长 | ☐ |
| W-704 | 睡眠 1 分钟后唤醒 | 动画和计时恢复，不瞬移、不加速 | ☐ |
| W-705 | 连续运行 2 小时 | 无崩溃、黑屏、严重闪烁或托盘失效 | ☐ |

## 12. 安装、升级与卸载

NSIS 和 MSI 分开执行完整流程：

| 编号 | 操作 | 预期 | 结果 |
|---|---|---|---|
| W-801 | 全新安装 | 安装完成并可启动 | ☐ |
| W-802 | 用更高 Test version 覆盖安装 | 升级成功，设置保留 | ☐ |
| W-803 | 从 Windows 设置卸载 | 程序文件和启动项清理 | ☐ |
| W-804 | 卸载后确认用户配置 | 记录配置是否保留，作为产品决策依据 | ☐ |
| W-805 | 重新安装 | 可以正常启动，不受旧残留阻塞 | ☐ |

## 13. Bug 回报模板

优先在 GitHub 使用 **Windows 11 测试问题** 表单；也可以把下面内容和附件直接发到当前 Codex 任务：

```text
Commit / Test version：
Windows 版本与架构：
安装包：NSIS / MSI
显示器与缩放：
角色：

问题：
复现步骤：
1.
2.
3.

预期结果：
实际结果：
发生频率：每次 / 经常 / 偶尔 / 一次
重启应用后：仍有 / 消失
重启 Windows 后：仍有 / 消失 / 未测试
附件：截图 / 10～30 秒录屏
```

每轮修复闭环：

```text
Windows 发现问题
→ 回报 Commit、Test version、DPI、步骤和录屏
→ Mac 端修复并提交
→ GitHub Actions 生成更高 Test version
→ Windows 覆盖安装复测
→ 记录通过或继续回报
```
